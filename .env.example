# Copy to .env.local

# Alchemy Starknet RPC key only (not the full URL). Server-side — never NEXT_PUBLIC_.
PROVIDER_URL=

# Redpocket helper — filled by the project after deploy. Users do not deploy this.
NEXT_PUBLIC_REDPOCKET_MAINNET=0x0
NEXT_PUBLIC_REDPOCKET_SEPOLIA=0x0
